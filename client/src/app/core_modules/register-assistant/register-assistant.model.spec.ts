import { RegisterAssistantModel } from './register-assistant.model';

describe('RegisterAssistant', () => {
  it('should create an instance', () => {
    expect(new RegisterAssistantModel()).toBeTruthy();
  });
});
