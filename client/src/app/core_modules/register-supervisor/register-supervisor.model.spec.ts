import { RegisterSupervisorModel } from './register-supervisor.model';

describe('RegisterSupervisor', () => {
  it('should create an instance', () => {
    expect(new RegisterSupervisorModel()).toBeTruthy();
  });
});
